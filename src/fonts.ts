// The handwritten face, asked for by the home page alone: it sets the two
// lines that say how often she does each thing, and nothing else. Loading it
// beside the running text would put 50 kB on the pages that never draw it.
import '@fontsource/caveat/latin-600.css';
