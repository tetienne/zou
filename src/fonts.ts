// Bundled with the site rather than fetched from a font CDN: a school machine
// is often behind a proxy that blocks fonts.googleapis.com, and a page that
// falls back to the system stack loses the voice the design is built on.
//
// Latin only, and one weight per role — a face nobody asked for is bytes the
// teacher waits on. Fraunces sets the titles, Karla the running text, and
// Caveat the one handwritten line on each card.

import '@fontsource/fraunces/latin-700.css';
import '@fontsource/karla/latin-400.css';
import '@fontsource/karla/latin-700.css';
import '@fontsource/caveat/latin-600.css';
